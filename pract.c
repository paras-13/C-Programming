#include <stdio.h>
#include <string.h>
int main()
{
    int arr[4] = {1,2,3,4};
    // char s[] = "Hello, World!";
    // char st[];
    // int length = 0;
    // length = strlen(s);
    // float a=3;
    // int b=2;
    // float div = a/b;
    // scanf("%sCharacter :- \n",&st);
    // printf("%s\nLength is :%d",s,length);
    printf("%d\n",arr[2]);
    return 0;
}